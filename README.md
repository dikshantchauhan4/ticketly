# Ticketly

React ticket management app for businesses with departments, teams, role-based dashboards, ticket transfers, chat history, image attachments, and TAT tracking.

## Run

```bash
npm install
npm run dev
```

Data is currently persisted in browser IndexedDB. See `DATABASE_SCHEMA.txt` for a server database migration map.
